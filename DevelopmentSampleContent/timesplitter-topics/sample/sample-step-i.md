<Note>
Make sure students are ready
</Note>

```json
{
  "name": "Dyson",
  "game": "cups"
}
```

### Final Step

```javascript
function go(car) {
  const n = 2;
  const arr = 5;
  return `go ${car} go go go`;
}

go("van");
```
