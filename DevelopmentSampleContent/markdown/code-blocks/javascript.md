# JSON Example

```json
{
   "name": "Dyson",
   "game": "cups"
}
```

# JavaScript Example

```javascript
function go(car) {
   const n = 2;
   const arr = 5;
   return `go ${car} go go go`
}

go("van");
```

