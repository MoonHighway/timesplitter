import styled from "styled-components";

export default function TopicTime() {
  return <Container>&lt;TopicTime /&gt;</Container>;
}

const Container = styled.div`
  grid-area: 1 / 1 / 2 / 2;
  background-color: orange;

  display: flex;
  align-items: center;
  justify-content: center;
`;
